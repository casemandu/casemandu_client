export const metadata = {
  title: `Track Order`,
  openGraph: {
    title: `Track Order`,
  },
}

const TracKOrderLayout = ({ children }) => {
  return <>{children}</>
}

export default TracKOrderLayout
